# Runtime dependencies verified in the current virtual environment.
# Python 3.13.9

# HTTP/API
fastapi==0.111.0
uvicorn==0.51.0
pydantic==2.12.2
pydantic-settings==2.14.2
slowapi==0.1.10

# Agent orchestration and model integration
langgraph==1.2.9
langchain==1.3.14
langchain-core==1.4.9
langchain-openai==1.3.5

# Checkpoint and local persistence
langgraph-checkpoint-sqlite==3.1.0
aiosqlite==0.22.1

# Official MCP SDK
mcp==1.28.1

# Real host telemetry
psutil==7.1.3

# Milvus 混合检索（dense + BM25 sparse + RRF）— RAG 生产主路径。
# langchain-text-splitters 提供 RecursiveCharacterTextSplitter。
# pymilvus + langchain-milvus 为 Milvus 官方 SDK / LangChain 集成。
#
# 版本依据（官方兼容性）：
#   - langchain-milvus 0.4.0 强制 pymilvus>=3.0.0,<4.0（PyPI requires_dist）。
#   - pymilvus 3.0.0 官方兼容 Milvus server 2.6.*（official compatibility matrix）。
#   - Milvus server：v2.6.21（Docker Standalone，docker-compose.yml）。
#   - RRF reranker：使用 RRFRanker(k=60)（BaseRanker），而非
#     Function(FunctionType.RERANK)——后者在 Milvus 2.6 上报
#     "unsupported rerank function: []"（走 function_score 路径，2.6 不支持）。
#   - 已知 langchain-milvus 0.4.0 bug：构造器不传 timeout（add_embeddings 会重复传参）。
#   - milvus-lite 已移除：Windows 不支持（sys_platform != "win32"），
#     且生产路径必须指向 Docker Standalone，禁止回退到 Lite。
langchain-text-splitters==1.1.2
langchain-milvus==0.4.0
pymilvus==3.0.0

# sentence-transformers 已不再是主路径依赖（Milvus 负责 sparse + dense 检索）。
# 暂保留以供可能的 local-embed 探索；若确认无需则下次清理。
sentence-transformers==5.6.0

# Test dependencies
pytest==8.4.2
httpx==0.28.1
