# 从不知道怎么学 Agent，到建立可验证学习系统

> 文档类型：学习方法形成母稿  
> 状态：第一版，基于当前文档与历史任务复原  
> 复核日期：2026-07-17  
> 适用范围：记录本轮 Agent / LLM 应用 / 后端项目学习方法的形成过程  
> 重要边界：这套方法已经改善学习方向与秩序，但项目尚未完整跑完全部链路，不能写成已经长期充分验证的万能公式

## 0. 这份母稿怎样处理证据

这不是一篇为了显得完整而补出来的成功故事。为了避免把后来的成熟总结伪装成一开始就有的计划，全文区分三类内容：

1. **用户明确表达**：用户在历史任务中直接说过的需求、判断或纠正。
2. **文档可证事实**：当前文件、进度记录或代码静态审查能够证明的状态。
3. **事后提炼**：根据前后因果总结出的模式。它有解释力，但不是用户当时的逐字原话。

本文不编造对话、日期、运行结果、生产指标或已经掌握的能力。引用用户表达时只保留能够从历史任务核对的短句；涉及代码质量时，区分静态审查发现与真实运行证据。

## 1. 先说结论：我不是找到了一个神奇 Prompt

这套方法不是一次设计出来的。

它来自一连串具体的不舒服：

- 项目文件越来越多，但我不知道自己到底学会了什么。
- AI 能很快生成完整目录和文档，但没有运行过的代码仍可能有严重错误。
- 一个窗口里讨论得很清楚，换窗口后上下文和约定又丢了。
- 我一度以为学习源码就是逐行看懂、再凭记忆敲一遍，但这既低效，也不像真实开发。
- 即使有严格的阶段和验收，如果路线本身选错，仍然可能非常有条理地学偏。
- 社交平台上的热门经验能提醒我市场在问什么，却不能替代官方文档和真实运行来证明技术正确。

每遇到一次这样的摩擦，我就把它写成一条更明确的规则；规则再进入文档，文档再约束下一个窗口。最后形成了：

- 三份核心控制文档；
- 四阶段学习闸门；
- 三条持续反馈回路；
- 一套“AI 大量执行、人工保留判断和验收权”的协作方式。

`AGENTS.md` 是思路真正开始变清晰的关键转折点，但它不是故事的起点。

## 2. 形成过程

### 2.1 起点：我先需要一个真实问题，而不是一套抽象课程

**用户明确表达**

我最初并不是先想清楚了完整学习体系，而是“不知道 Agent 开发该从哪里学、怎样才算学会”。因为找不到合适的真实需求，我先找了一个足够具体的需求作为学习载体。

具体业务、早期背景或某个操作系统并不是方法的核心。真正有价值的是：它提供了用户输入、模型判断、工具执行、状态、安全、API 和错误处理等真实工程边界，让学习不再停留在“调用一次模型 API”。当前项目已经明确转为求职作品主线，旧的比赛或平台约束不能继续决定工程标准。

**事后提炼**

真实需求的价值不在于题目多特别，而在于它会迫使学习者面对约束、失败和取舍。一个需求只是载体，不应反过来绑死以后所有 Demo 的业务和平台。

同理，把项目适配到另一种操作系统，也不是简单替换数据库里的命令。核心架构可以复用，但仍需适配：

- 工具实现与系统命令；
- 路径和文件系统语义；
- 权限与进程模型；
- 服务、日志和健康检查语义；
- 依赖、部署与测试。

数据库在这个项目里主要承担状态、检查点和审计等职责，不是操作系统适配层。

### 2.2 第一次加速：AI 很快给出了“完整项目”

**文档可证事实**

早期版本已经拥有相当完整的目录：

- API 入口；
- Agent 编排；
- 模型适配；
- Tool Registry 与系统工具；
- Safety Guard；
- MCP、审批、审计和记忆；
- Web 前端；
- 部署脚本；
- 多篇学习日志。

`PROJECT-CONTEXT.md` 一度把多个 Phase、MCP、审批和 UI 标成已完成，并估计项目总体约完成 75%；同时它也明确记录自动化测试、测试报告、演示材料和真实环境部署验证尚未完成。

**用户明确表达**

项目大量代码由 AI 生成。后续由 Claude Code 接 LongCat 生成的 LangGraph / LangChain 版本，也不是一次成功实现。

**事后提炼**

AI 生成速度会制造一种很强的完成感：目录有了、模块有了、注释有了、文档也能解释每个模块，于是很容易把“结构完整”误认为“系统成立”。

但这些东西只能证明 AI 生成过内容，不能证明：

- 真实入口能够走到模块；
- 路由、状态和工具协议一致；
- 框架 API 与安装版本兼容；
- 安全和审批真的进入主链路；
- 错误路径能够恢复；
- 用户本人能够解释和修改。

这是后来“证据不足不得宣布完成”规则的来源。

### 2.3 为了解决跨窗口失忆，我先有了上下文书和学习日志

**文档可证事实**

`PROJECT-CONTEXT.md` 的目标是把项目定位、目录、API、完成项、待办和操作规则保存下来，供新窗口或另一个 Agent 快速接管。

学习日志则采用类似以下结构：

- Concept；
- In Our Code；
- Why It Matters；
- Common Pitfalls；
- Further Reading。

它们把沙盒、安全护栏、MCP 和审批流等概念映射回项目文件，确实改善了“窗口一换就要重新解释”和“概念不知道落在哪段代码”的问题。

**局限**

这些文档主要解决的是“AI 和人怎样恢复上下文”，还没有解决“用户是否已经掌握”。

早期日志里存在一些后来需要修正的情况：

- 某些能力被写成“已生效”或“已修”，但缺少端到端运行和测试证据；
- MCP、审批和安全模块的存在容易被当成完整闭环；
- 当时的框架 API 与后来的官方主线发生变化；
- 文档讲解很完整，但没有用户独立回答、运行、排错和迁移的证据。

因此，学习日志是学习材料，不是掌握证明；上下文书是交接材料，不是真实运行报告。

### 2.4 第一次认知转折：优秀 Agent 项目到底应该看什么

**用户明确表达**

我曾经问过一个很关键的问题：即使找到优秀 Agent 项目，我到底要从里面学什么？看功能、看代码，还是看我当时还说不清的“架构设计”？

**文档可证事实**

`docs/how-to-study-agents.md` 把观察对象整理成六个维度：

1. Agent Loop；
2. Tool Registry；
3. Context Management；
4. Permission / Safety；
5. Model Abstraction；
6. UI / Session。

历史任务随后又补上了运行时轨迹、完成判定、失败恢复、Prompt 协议、State / Memory、Observability、Eval、HITL、安全边界、成本与延迟。

**事后提炼**

这次讨论把学习问题从“我要读哪个著名项目”改成了：

> 为了理解一个机制，我应该沿哪些文件和运行证据去找答案？

优秀项目不是用来收藏，也不是用来从第一行读到最后一行。更高效的方法是带着一个明确问题，找到入口、核心循环、状态、工具、权限和验证点，再回到自己的项目做一次小迁移。

这一步还不完整。早期方法偏静态架构，后来才加入真实 Run、Trace、评测和 Badcase，才从“看懂骨架”走向“证明行为”。

### 2.5 从逐行阅读和默写，转向调用链、小改动和真实验证

**用户明确表达**

我问过：Agent 生成的代码是不是每一行都要看懂？看完后要不要凭记忆自己再敲一遍？我也明确要求，Agent 开发、后端开发和代码能力提升都应基于真实开发环境、真实项目和可运行工程流程，不把重点放在脱离实践的底层。

历史任务给出的学习循环是：

```text
跑起来
→ 找一条链路
→ 看懂输入和输出
→ 改一个小点
→ 运行验证
→ 记录学到了什么
```

**形成的阅读分层**

- 权限、命令执行、文件修改、密钥、数据库写入等高风险代码需要精读。
- API、服务、状态、模型适配和业务分支需要按函数或关键代码块理解。
- 样板、普通 UI 和暂时不触碰的框架内部实现可以先略读。

**事后提炼**

“能背着重写”不是唯一、也通常不是最好的掌握证据。更接近真实工作的能力是：

- 知道请求从哪里进、结果从哪里出；
- 能找到正确修改点；
- 能做一个小改动而不破坏相邻行为；
- 能看日志、Trace、测试和错误栈；
- 能解释为什么这样设计；
- 能把同一机制迁移到一个新场景。

### 2.6 为什么主线转向 LangGraph / LangChain 等成熟框架

**用户明确表达**

求职方向是 Agent 开发、后端开发和 LLM 应用开发。用户明确批评过一种偏差：明明主流框架已经提供能力，AI 却自行重写底层，“别人搞框架就是为了省功夫的，但你却自己找事干”。

这后来形成了根目录规则：

- 优先成熟、主流、仍在维护的框架；
- 只有框架不适合、或底层知识直接服务于排错和架构决策时，才下沉实现；
- 遇到版本敏感问题时查当前官方文档；
- “能运行”只是最低标准，还要可理解、可测试、可观测、可维护。

**文档可证事实**

项目后来保留旧版手写实现作为对照和诊断材料，主学习路径转向 `app_v2` 的 LangGraph 工程链路；现代 LangChain `create_agent` 则被定位为薄对比，而不是再维护一套完整产品。

**事后提炼**

这不是“LangGraph 永远最好”，而是学习目标发生了纠正：

> 学的是怎样借助成熟框架交付可靠 Agent 应用，而不是从零造一个较弱版本的框架。

普通代码、固定 Workflow、RAG、高层 Agent API 和 LangGraph 都应由需求决定，不能先选框架再反向套需求。

### 2.7 第二次关键教训：AI 生成的主流框架版本仍然可能错得很具体

**文档可证事实**

静态审查在 `app_v2` 和 `app_v3` 中发现了多类阻断级或严重问题，包括：

- LangGraph 条件路由返回值与映射键不一致；
- 当前用户输入没有进入某版 Prompt；
- 使用旧 Agent API，却允许安装不兼容的新框架版本；
- 普通字典列表错误使用消息 Reducer；
- 残留生成占位符导致语法错误；
- 匿名请求共用默认 Thread，存在状态串线风险；
- 审计、审批和工具输出扫描模块存在，但没有真正进入主流程；
- 正则手拆模型 JSON；
- 模拟日志被标成真实成功；
- 路径安全边界扩大；
- 测试、依赖锁定和真实端到端运行缺失。

`v1 vs v2.md` 也明确写着“完整但未运行验证的骨架”，同时对某些当时认为正确的框架用法表达得过于肯定；后来官方资料核对又证明其中存在旧 API 和概念边界问题。

**结论**

换成主流框架并不会自动带来正确性。AI 会把“当前教程、旧版本记忆、看起来合理的模块结构”组合成一份很像工程项目的代码，但只有当前官方资料、导入、运行、测试、API 调用和 Trace 能把它拉回事实。

因此，AI 不是绝对正确的老师。它可以加速扫描、实现和排错，但不能同时充当唯一作者、唯一审核者和最终验收者。

### 2.8 AGENTS.md 的诞生：转折点不是一句规则，而是一连串摩擦

`AGENTS.md` 不是整个过程的起点，却是方法开始有稳定形状的关键转折点。

#### 第一步：承认聊天记忆不可靠

**用户明确表达**

用户先问：之前要求加入的长期记忆是否会被所有沙盒读取。历史任务中，AI 明确承认无法保证平台级长期记忆。

用户随后提出：至少在当前工作区创建一份可审核的 Markdown，让以后切换多少窗口，都能通过读取文件恢复要求。

于是根目录第一次创建 `AGENTS.md`。

#### 第二步：写入真实工程方向

最初版本保存：

- 求职方向；
- 真实开发环境、真实项目和可运行流程；
- 优先主流框架；
- 不无理由重造底层；
- 配置、错误、安全、测试、观测和维护等工程要求；
- AI 既是老师也是协作者，但必须对方向保持诚实。

#### 第三步：把“问题拆解”升级为强制规则

**用户明确表达**

用户提出：问题拆解和细化非常重要。按链路划分后，学习会更清楚；每次真正弄懂当前任务再进入下一步，就不用反复回头梳理已经进行到哪里。

这使 `AGENTS.md` 从偏好清单变成了过程控制：

- 项目拆成链路；
- 链路拆成阶段；
- 阶段拆成目标、任务、依赖和验收；
- 当前范围和进度始终可见；
- 后续问题先记录，不在当前阶段一次性灌入。

#### 第四步：用户亲自提出四阶段和阶段门

**用户明确表达**

用户的原始规则是：项目先拆成很多条链路；每条链路分阶段；“只有这个阶段搞好之后才能进行下一步”；进入下一阶段前要先提问或检查，没学会就不能推进。

在交互中，四阶段逐渐固定为：

1. 链路认知；
2. 改造与读码；
3. 真实运行与测试；
4. 复盘与迁移。

#### 第五步：把验收质量单独写成标准

用户进一步强调：每条链路、每个阶段的验收“十分重要”，如果是提问，提问质量本身也很关键。

于是规则不再只是“阶段末问几道题”，而是：

- 从当前学习目标反向设计验收；
- 测理解、预测、操作、排错和迁移，不测无关记忆；
- 先让用户独立作答，再逐级给提示；
- 部分回答既不自动失败，也不自动通过；
- 只补具体缺口，并换一道题复验；
- 每个关键目标都要有证据；
- 最后一阶段必须包含未见过的迁移任务。

#### 第六步：加入求职与外部校准

市场调研后，`AGENTS.md` 又长出：

- 薄端到端纵切优先；
- 链路要有合理时间盒；
- 基础知识必须说明它服务的用户失败、工程决策或面试场景；
- 里程碑要同时看最新市场证据和当前官方文档；
- 高频面试问题要映射到项目链路、证据和 Stage 4；
- 不适合塞进主产品的后端或模型基础可以做可运行 companion lab；
- 新 JD、面经和真实面试反馈用于持续校准。

#### 第七步：加入跨窗口防丢和面试笔记

后来又增加了一条更谨慎的沉淀规则：项目面试笔记不能一开始就写成通用八股，只能在真实问题、修改、测试、Trace 或指标出现后，记录实际经历和个人贡献。

这说明 `AGENTS.md` 不是一次写好的宪法，而是一份从实际失败中长出来的协作记忆。

### 2.9 AGENT-CHAIN.md：把规则实例化成当前项目状态

有了长期规则，仍然缺少“这个项目现在学到哪里、最终要学到哪里、下一道门是什么”。

**文档可证事实**

最初路线图把主执行流程拆成 12 条可验收链路，后来经过市场校准扩展到 17 条，并按 V0 至 V4 的薄纵切重新排序。

`AGENT-CHAIN.md` 现在承担：

- 项目术语和主执行流程；
- 链路清单、依赖和状态；
- 当前链路、当前阶段和下一道闸门；
- 每条链路的工程验收目标；
- 端到端场景；
- 四阶段学习协议和验收记录要求；
- 已知代码基线问题；
- 面试领域与链路、场景、证据的映射；
- 跨窗口交接和未完成项。

它解决的是“现在具体做什么”，而不是永久规则。

当前文档仍把项目标记在链路 01、阶段 1；大部分链路和端到端场景仍为待开始。这一点非常重要：有了路线图不等于已经走完路线。

### 2.10 第一次执行链路 01：发现流程严谨也可能学偏

新窗口严格读取规则和路线图后，正式从链路 01 的依赖与外部配置开始。过程完全符合规则：

- 范围清楚；
- 只读指定文件；
- 不提前改代码；
- 给出验收问题；
- 回答后再决定是否推进。

但用户很快提出了关键纠正：

> “你得从 Agent 开发工程师的角度来看，你确认这里有意义么？我觉得没啥意义，这个就是简单的科普一下给我就好。”

用户希望重点放在 Agent 的设计、主流框架、业务逻辑和真实工程，而不是在第一个子步骤花大量时间背依赖和环境变量。

后续又发现：

- `checkpointer.py` 单文件只展示资源创建，无法单独证明多轮、隔离和恢复；
- 真正的价值要跨 `compile(checkpointer=...)`、`thread_id`、State 和测试观察；
- 当前项目缺少的能力不能因为代码没写就从学习路线消失，必须记录为后续补充项；
- 旧版底层实现只应在框架行为令人困惑或排错时做对照，不应主导学习顺序。

**事后提炼**

这是整个方法的第二层成熟：

> 严格流程只能保证“按计划执行”，不能保证“计划方向正确”。

四阶段协议本身没有错，问题在于链路范围过大、顺序过于串行。若一个基础链必须完成全部生产级细节才能通过，用户会在很久以后才第一次看到 Agent 行为。

因此后来加入：

- 链路时间盒；
- V0 / V1 薄纵切；
- 测试和 Trace 随每个纵切出现；
- 深化项延后但不丢失；
- 每个里程碑做市场与版本校准。

### 2.11 用真实面经和官方资料校准路线

**文档可证事实**

`INTERVIEW-MARKET.md` 调研了牛客、小红书和抖音共 18 篇网页版原帖，并用 LangChain、LangGraph、LangSmith 和 MCP 官方资料核验版本敏感结论。

报告没有得出“底层都没用”，而是更精确地指出：

- 基础工程方向合理；
- 执行顺序失衡；
- 核心 Agent 行为、测试和 Trace 出现得太晚；
- RAG、评测回归、上下文、延迟和成本缺少独立所有者；
- 安全重要，但不应在第一次可运行 Agent 之前无限展开；
- 现代 `create_agent` 应提前做薄对比；
- 后端、并发、数据库和缓存仍是相邻岗位的重要能力。

路线由近乎串行的“先做完所有基础设施”调整为：

- V0：最小启动；
- V1：真实 Agent 纵切；
- V2：RAG 与评测；
- V3：可靠与可控；
- V4：性能与交付。

**三条证据规则**

1. 面经用于发现频率和场景，不用于证明 API 正确。
2. 官方文档用于核对当前框架能力和版本，不用于证明用户已经掌握。
3. 最终掌握仍需本项目中的代码、测试、Run、Trace、指标、解释和迁移证据。

这一步补上了此前缺少的外部反馈：否则一套内部流程即使越来越严谨，也可能只是在优化自己的假设。

### 2.12 从“窗口结束时总结”升级为主动检查点

**用户明确表达**

在 `app_v4` 修复过程中，Claude Code 目标模式的上下文窗口被跑满。下一轮如果没有可靠交接，就需要重新扫描代码、重新判断进度、重新生成提示词，既浪费 token，也容易把已经达成的结论弄丢。

用户提出的关键问题是：能不能让 Agent 在上下文快满时主动生成总结和进度文档，让下一个过程继续接着干，而不是每次都从头审一遍。

**已有基础**

此前 `AGENT-CHAIN.md` 和 `AGENTS.md` 已经要求在切换任务或上下文压缩前保存交接信息，包括当前链路、阶段、文件、命令、证据、未解决问题和下一道关卡。

这说明方向是对的，但规则还不够主动：如果只等到窗口真的快满，模型往往已经没有足够空间做高质量总结。

**新形成的方法**

上下文交接不应只是一条“结束时总结”的礼貌规则，而应成为长任务里的主动检查点机制。

1. 每完成一个可验收行为，就更新一次项目事实源。
   例如测试通过、API 跑通、Trace 产生、拒绝策略生效、MCP 工具可被外部客户端调用，这些都要进入当前项目的路线图或进度文档。

2. 每经过一段较长工作，就写一次轻量检查点。
   默认节奏可以是约 20 次工具调用、约 45 分钟真实工作、完成一个子目标，或发现路线变化时。检查点不追求长，而追求可恢复。

3. 当上下文明显变长时，主动收束任务。
   如果能感知上下文比例，约 60% 后不再扩张新需求，约 70% 后优先验证、记录、交接。如果不能感知比例，就用对话长度、工具调用次数、文件扫描量和任务复杂度估算。

4. 下一步最多写 3 个动作。
   交接文档最重要的不是复述所有历史，而是让下一个窗口知道从哪里继续。下一步过多会让新窗口再次发散。

5. 交接前必须留下第一条可执行命令。
   例如 `git status --short`、`pytest app_v4/tests -q`、`python -m app_v4.mcp_server`。这样下个窗口可以先用最小成本验证状态，而不是重新全量审查。

**推荐写入位置**

项目级进度优先写进当前项目的路线图或状态文档。对 `kylin-os-agent` 来说，主事实源是 `D:\klin-agent\kylin-os-agent\AGENT-CHAIN.md`。

如果任务特别长，或者另一个 Agent 不可靠，也可以额外维护一个短文件，如 `HANDOFF-LATEST.md`。但它只能作为恢复胶囊，不能替代路线图。路线图仍然记录长期阶段、验收和证据。

**交接最小字段**

- 当前目标和禁止事项；
- 当前链路、阶段、验收门槛；
- Git 状态、分支、远端和未提交改动；
- 关键文件及其职责；
- 已执行命令和结果；
- 已产生证据，如测试、Trace、API 响应、Badcase；
- 失败或未完成事项；
- 已确认的设计决策；
- 下一步最多 3 个动作；
- 下个窗口第一条应该执行的命令。

**下个窗口的恢复顺序**

1. 先读 `AGENTS.md` 或 `CLAUDE.md` 这类长期协作规则；
2. 再读项目路线图或状态文档；
3. 再读最新 handoff；
4. 用 `git status`、关键 diff 和最小测试核验事实；
5. 只围绕 handoff 继续，不重新做无边界全量审计。

**重要边界**

主动检查点并不是“完全不消耗 token”。它会消耗少量 token 做记录，但能避免下一轮重新扫描、重新推理和重新提示词设计，整体上更省。

交接文档也不是绝对真相。下个窗口必须用最小命令验证关键事实，但不应该因为不信任交接而重新审完整个项目。

## 3. 现在形成的学习系统

### 3.1 三份核心文档

| 文档 | 它回答什么 | 何时读取 | 何时更新 | 它不能证明什么 |
|---|---|---|---|---|
| `AGENTS.md` | 长期方向、协作原则、学习规则和质量底线是什么 | 每个新任务、新窗口和重大决策前 | 只有跨项目稳定规则真正改变时 | 不能证明当前项目已经完成 |
| `INTERVIEW-MARKET.md` | 当前市场在问什么、路线是否偏航、哪些 API 需要官方核验 | 里程碑、求职方向变化、新 JD 或真实面试反馈出现时 | 有日期的新证据形成时 | 不能凭一篇热门帖子证明技术正确 |
| `AGENT-CHAIN.md` | 当前项目学到哪里、下一步是什么、最终证据和验收门是什么 | 每次进入当前项目和每个阶段边界 | 阶段开始、通过、阻塞、范围或优先级改变时 | 写入路线不等于已经掌握 |

三者关系不是并列的三个笔记，而是：

```text
AGENTS 定长期方向和规则
        ↑          ↓
INTERVIEW-MARKET 用外部证据校准优先级
        ↓
AGENT-CHAIN 把规则实例化为当前项目的路线、进度和验收
```

可以把它们记成：

```text
定方向 → 看外界 → 做当前
```

### 3.2 三层文档资产

随着方法继续发展，文档可以放进三个层级：

1. **长期治理与方法资产**
   - `AGENTS.md`
   - `agent demo开发流程.md`
   - 本文 `LEARNING-METHOD-FORMATION.md`

2. **外部证据与方向校准**
   - `INTERVIEW-MARKET.md`
   - 新 JD、面经和真实面试反馈

3. **当前项目执行与验收**
   - `AGENT-CHAIN.md`
   - 测试、Eval、Trace、运行记录和后续证据型面试笔记

长期规则、市场证据和当前进度必须分开，避免一次热点变成永久规则，也避免短期项目状态污染跨项目方法。

### 3.3 四阶段学习闸门

#### 阶段 1：链路认知

目标：

- 找到边界、输入输出、关键文件、调用顺序、依赖和当前缺陷；
- 不要求通读每一行；
- 不修改业务代码。

证据：

- 能重建调用顺序；
- 能判断文件职责；
- 能预测一个配置或状态变化会导致什么行为。

#### 阶段 2：改造与读码

目标：

- 只做当前链所需的小范围改动；
- 理解关键代码块、框架用法、设计原因和替代方案。

证据：

- 能沿最终代码解释主要分支；
- 能指出正确修改点；
- 能说明为何使用当前框架能力而不是自研。

#### 阶段 3：真实运行与测试

目标：

- 在真实开发环境安装、启动、调用、测试和排错；
- 用运行事实关闭“应该能跑”的猜测。

证据：

- 真实命令与输出；
- 自动化测试、API 检查和 Trace；
- 能从错误、日志或失败测试定位问题。

#### 阶段 4：复盘与迁移

目标：

- 脱离逐步提示讲清问题、架构、取舍和失败；
- 把机制应用到一个没有照抄过的新任务。

证据：

- 独立讲解；
- 新诊断、扩展或小修改；
- 说明自己的实际贡献和仍未掌握的部分。

### 3.4 三条反馈回路

#### 回路一：工程事实回路

```text
需求或坏例
→ 切出一条薄链
→ 实现
→ 运行、测试、Trace
→ 找到第一个真实偏差
→ 形成下一个最小改动
```

它防止文档、目录和 AI 自述替代系统行为。

#### 回路二：学习验收回路

```text
阶段学习目标
→ 用户独立回答或操作
→ 精确识别缺口
→ 只补这个缺口
→ 换题或新任务复验
→ 更新路线图证据
```

它防止“AI 讲完了”被误记为“用户学会了”。

#### 回路三：外部校准回路

```text
近期 JD / 面经 / 真实面试反馈
          +
当前官方框架资料
→ 更新带日期的市场证据
→ 检查 AGENTS 的长期原则是否仍成立
→ 重排 AGENT-CHAIN 的优先级与验收映射
```

它防止严格流程把人稳定地带向错误方向。

## 4. 用户和 AI 各自负责什么

### 4.1 用户不能外包的责任

- 提供真实目标、约束和优先级；
- 判断一个需求是否值得做；
- 审核 AI 对需求的复述；
- 决定架构、权限、密钥、数据和风险边界；
- 批准安装、删除、迁移、外部写入、Git 发布等真实动作；
- 沿调用链审核关键实现；
- 亲自运行、解释结果并完成迁移任务；
- 对“我是否真的掌握”给出诚实证据；
- 根据求职目标决定时间应该花在哪里。

### 4.2 AI 适合承担的责任

- 扫描项目和恢复上下文；
- 将模糊问题拆成链路、阶段和验收；
- 调研当前官方资料和候选方案；
- 起草 PRD、路线图、代码、测试、文档和评测样例；
- 执行允许范围内的格式、导入、测试、API 和 Trace 检查；
- 从错误和 Badcase 中提出归因候选；
- 设计高信号验收问题，并根据回答只补具体缺口；
- 保存进度、证据、未解决问题和下一道门。

### 4.3 AI 不能替代的证据

- “AI 已完成”不是运行证据；
- 生成代码的 AI 自己说代码正确，不是独立审核；
- 模块存在，不代表主链能到达；
- 文档说“已测试”，不代表命令真实执行；
- AI 给出标准答案，不代表用户能独立回答；
- 一篇高赞面经，不代表框架 API 正确；
- 一个漂亮 Demo，不代表失败、隔离、安全和成本都可控。

最合适的关系不是“AI 当绝对正确的老师”，也不是“人逐行手写所有代码”，而是：

> AI 扩大搜索、实现和检查能力；人保留目标、判断、责任和最终验收权。

## 5. 重要反面经验

### 5.1 不要把“模块齐全”当成“项目完成”

目录、类、接口、注释和架构图只能证明结构存在。真实入口、错误路径、测试和 Trace 才能证明能力进入系统。

### 5.2 不要把“文档完整”当成“用户掌握”

上下文书和学习日志能保存信息，却不能替用户回答、运行、排错和迁移。

### 5.3 不要先通读错误代码

当代码包含旧 API、生成占位符和明显断链时，逐行学习会把错误模式一起吸收。先静态审查，修出最小可运行链，再读最终行为。

### 5.4 不要把逐行背诵当成代码能力

真实开发更需要找入口、追调用、改小点、运行验证和解释设计。局部复刻可以练手，整项目默写通常不是高收益方法。

### 5.5 不要因为用了主流框架就放弃审核

框架选择正确，代码仍可能因为版本、状态、路由、生命周期和接线错误而失败。

### 5.6 不要让严格流程掩盖错误方向

阶段门只能保证执行纪律，不能自动决定学习优先级。链路要时间盒化，里程碑要接受市场和官方资料校准。

### 5.7 不要把热点帖子直接写成永久规则

面经用于发现问题频率，官方资料用于确认当前 API，项目证据用于证明真实能力。三者不能混用。

### 5.8 不要为了覆盖所有题目把主项目做成杂物间

后端并发、MySQL、Redis、模型选型等可以用小型可运行实验补证据，不必全部硬塞进 Agent 主链。

### 5.9 不要过早抽象成万能 Skill 或平台

曾经出现过把学习规则封装成新 Agent 产品或 Skill 的想法。方向可能有价值，但方法尚未跑完一个完整闭环时，过早抽象会把未经验证的假设固化。先在当前项目完成真实验证，再提炼复用。

### 5.10 不要把跨平台迁移简化成替换数据库数据

数据库不是命令适配层。系统工具、权限、路径、服务与日志语义、依赖和测试都要分别适配。

## 6. 当前局限与未完成事项

截至本稿复核时：

- 当前项目路线仍在链路 01、阶段 1；
- 多数能力链和端到端场景仍为待开始；
- app_v2 / app_v3 的静态问题清单尚未逐项通过真实运行关闭；
- 四阶段协议尚未在所有链路完整跑完；
- 市场调研是有限样本，不能代表全部公司和岗位；
- “三份文档 + 四阶段 + 三反馈回路”已经改善秩序，但还需要多个不同项目验证；
- 后续面试笔记应只写有真实项目证据的经历，当前不能提前编造完整答案；
- 本文是在已有文档和历史任务上复原的方法史，仍可能遗漏更早、未被持久化的临时想法。

因此，对外最诚实的说法是：

> 我正在把这套方法放进真实项目验证。它已经帮助我从混乱走向可追踪、可验收和可纠偏，但还不是经过长期验证的万能公式。

## 7. 怎样在下一个项目复现

### 步骤 1：先找一个真实问题

写清：

- 谁在什么场景使用；
- 要得到什么可观察结果；
- 最坏失败是什么；
- 普通代码、Workflow 或 RAG 是否已经足够；
- 为什么需要或不需要 Agent。

不要因为想学某个框架就强造需求。

### 步骤 2：让 AI 先复述，而不是立刻生成项目

要求 AI 输出：

- 它理解的目标；
- 范围和非目标；
- 已知事实、假设和待确认点；
- 最小成功场景；
- 停止条件和人工审批点。

人先审核目标，再允许进入实现。

### 步骤 3：建立最小文档基线

至少保留：

- 项目稳定规则；
- 一页 PRD 或需求卡；
- 当前项目路线图；
- 最小测试 / Eval 契约；
- README 和真实运行证据。

文档服务于决策和交接，不按“显得专业”堆数量。

### 步骤 4：先审现状，再切薄纵向链路

如果已有 AI 生成代码：

1. 检查真实入口和调用链；
2. 核对当前官方 API；
3. 找出阻断错误、空心模块和假结果；
4. 先确定最简单的可运行基线；
5. 不一次性重写全部系统。

第一条纵切应尽快经过真实入口、模型、必要工具、最小状态、输出、Trace 和测试。

### 步骤 5：每条链执行四阶段

```text
链路认知
→ 改造与读码
→ 真实运行与测试
→ 复盘与迁移
```

每阶段都要写清：

- 学什么；
- 暂时不学什么；
- 什么证据才算通过；
- 失败时留在哪一步；
- 下一道门是什么。

### 步骤 6：把 AI 任务限制在可审核范围

每个实现任务给出：

- 背景、目标、范围、非目标；
- 允许修改和禁止修改的文件；
- 必须保持的行为；
- 验收标准和验证命令；
- 风险、费用和停止条件。

AI 完成后必须报告真实命令、结果、未运行项和剩余风险。

### 步骤 7：让用户亲自留下掌握证据

每个阶段先让用户独立：

- 复述调用链；
- 预测行为；
- 运行命令和解释输出；
- 从 Trace 或失败测试定位问题；
- 完成一个新场景的小修改。

AI 只针对缺口补课，再换题复验。

### 步骤 8：里程碑做外部校准

同时检查：

- 最新 JD、面经和真实面试反馈；
- 当前官方框架文档；
- 项目真实 Badcase、指标和时间成本。

需要改变优先级时，先更新带日期的外部证据，再修改项目路线；只有跨项目稳定结论才进入长期规则。

### 步骤 9：每次切换窗口前保存可继续状态

路线图至少记录：

- 当前链路和阶段；
- 已读 / 已改文件；
- 运行过的命令；
- 证据和结论；
- 未解决问题；
- 下一道验收门；
- 已经足够形成面试笔记的真实案例。

不要让聊天窗口成为唯一记忆。

### 步骤 10：完成后再抽象和分享

只有方法在多个真实 Demo 中跑过，才考虑：

- 提炼成通用手册；
- 封装为 Skill 或工具；
- 总结为面试案例；
- 对外分享为文章。

分享时保留失败、变化和局限，不把迭代过程包装成一开始就设计好的成功学。

## 8. 对外文章准备

### 8.1 三个标题候选

1. **我终于知道该怎么学 Agent 了**
2. **从乱学 Agent 到建立验收规则**
3. **没有神奇 Prompt，只有不断纠偏**

采用标题：**我终于知道该怎么学 Agent 了**

选择理由：它对应真实起点，不夸大成果，也把重点放在学习方法而不是某个工具或框架上。

### 8.2 小红书正文

我原本连 Agent 开发该从哪里学、怎样才算学会都不知道。

最开始，我只是因为找不到合适的真实需求，就先找了一个具体项目当学习载体。那时我的想法很简单：先让 AI 把项目做出来，我再跟着代码学。

AI 的确很快。目录、模块、接口、学习文档，看起来一下子全都有了。我一度会因为“项目已经很完整”而安心。

但后来我发现，文件多不等于项目能跑，文档完整也不等于我学会了。

有些模块只是写出来，却没有进入真实主链路；有些代码用了过时的框架 API；还有一些看起来很合理的功能，实际上从来没有运行和测试过。更直接的问题是：换一个聊天窗口，前面讨论的目标和规则还可能丢失。

我先用项目上下文和学习日志解决了一部分记忆问题。它们能告诉下一个窗口：项目有什么、做到了哪里、每个概念对应哪些代码。

可这还不够。它们能证明“写过”，不能证明“我会了”。

后来我问了一个对我很重要的问题：看一个优秀 Agent 项目，我到底要学什么？

不是把源码从第一行看到最后一行，也不是看完再凭记忆默写一遍。我真正需要看的是：一次请求怎么进入 Agent，模型怎么决定下一步，工具怎么注册和执行，状态怎么保存，权限在哪里判断，失败后怎样恢复。

学习方式也慢慢变成了：

跑起来 → 找一条调用链 → 看懂输入输出 → 改一个小点 → 运行验证 → 记录问题。

真正的转折点，是我把这些不断出现的“不对劲”写进了 `AGENTS.md`。

它不是故事的起点，而是我的思路开始稳定下来的地方。

一开始，它只记录几个长期要求：学习必须基于真实项目和真实环境，优先使用主流框架，不要明明有成熟方案还自己重造底层。

后来我又把新的问题继续写进去：

项目要拆成链路；每条链路要分阶段；当前阶段没有真正通过，就不能因为 AI 已经讲完了而自动进入下一步。

最终形成了四个阶段：

1. 链路认知：看懂边界、输入输出、文件和调用顺序。
2. 改造与读码：只改当前链，理解关键代码和设计取舍。
3. 真实运行与测试：亲自启动、调用、跑测试、看 Trace 和错误。
4. 复盘与迁移：脱离提示讲清楚，再完成一个没见过的小任务。

但第一次严格执行时，我又发现了一个新问题。

流程可以很严谨，方向却仍然可能偏。

我们当时从依赖和配置开始，一步一步都很规范，可我很快意识到：如果目标是 Agent 开发，为什么要在这些基础细节上花这么久，真正的 Agent 行为却迟迟没有出现？

这让我明白，阶段闸门只能保证“按计划学习”，不能保证“计划本身正确”。

所以我又加入了第二种校准：去看真实岗位和面经到底在追问什么，再用官方文档核对当前框架做法。

现在三份文档分别解决三个问题：

- `AGENTS.md`：长期方向和学习规则是什么。
- `INTERVIEW-MARKET.md`：外面的岗位在问什么，我有没有有条理地学偏。
- `AGENT-CHAIN.md`：当前项目学到哪里，下一步是什么，怎样才算通过。

AI 在这个过程中帮我扫描代码、拆问题、写草稿、补测试和找资料，但我不再把 AI 当成绝对正确的老师。

需求是否合理、框架是否值得用、权限和风险是否能接受、我是否真的学会，这些判断必须由我保留。

我现在越来越相信一件事：突发的 idea 如果只停留在脑子里，很快就会消失；及时写成文字，它才可能跨窗口保存，被下一次实践修正，最后变成可以复用的方法。

这套方法还没有跑完所有项目链路，我也不会把它说成万能公式。

但它已经让我从“我应该学什么”走到了“我现在学到哪、证据是什么、哪里可能学偏、下一步为什么这样走”。

对我来说，这已经是一次很重要的变化。

#Agent开发 #LLM应用开发 #AI编程 #项目式学习 #学习方法
